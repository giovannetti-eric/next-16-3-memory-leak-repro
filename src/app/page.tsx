export default function Home() {
  return (
    <main>
      <h1>next 16.3 memory leak reproduction</h1>
      <p>
        Build, start, then run <code>pnpm load</code>. Hit <code>/any-slug</code> to see one page.
      </p>
    </main>
  );
}
